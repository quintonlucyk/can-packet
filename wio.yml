type: pkg

project:
  name: cosa-packet
  version: 0.0.1
  keywords:
  - wio
  - pkg
  compile_options:
    wio_version: 0.5.1
    default_target: tests

targets:
  tests:
    src: tests
    platform: AVR
    framework: cosa
    board: uno

